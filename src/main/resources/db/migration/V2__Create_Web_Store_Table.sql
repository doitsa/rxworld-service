CREATE TABLE web_store (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  organization VARCHAR(255),
  doit_url VARCHAR(255),
  rxworld_url VARCHAR(255),
  username VARCHAR(255),
  password VARCHAR(255),
  client_id VARCHAR(255),
  client_secret VARCHAR(255),
  order_criterias VARCHAR(255) DEFAULT 'orderplaced',
  doit_web_store_id INTEGER,
  rxworld_config_id INTEGER,
  CONSTRAINT pk_web_store PRIMARY KEY (id)
);

ALTER TABLE web_store ADD CONSTRAINT fk_web_store__rxworld_config_id FOREIGN KEY (rxworld_config_id) REFERENCES rxworld_config;
ALTER TABLE web_store ADD CONSTRAINT uq_web_store__doit_web_store_id__organization UNIQUE (doit_web_store_id, organization);
